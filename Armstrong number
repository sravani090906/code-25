#include <stdio.h>
int main() {
    int n=153, temp=n, sum=0, r;
    while(n>0) {
        r=n%10;
        sum+=r*r*r;
        n/=10;
    }
    if(sum==temp)
        printf("Armstrong");
    else
        printf("Not Armstrong");
    return 0;
}
